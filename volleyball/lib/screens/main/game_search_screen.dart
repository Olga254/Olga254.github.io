import 'package:flutter/material.dart';

class GameSearchScreen extends StatelessWidget {
  const GameSearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Поиск игр')),
      body: const Center(child: Text('Экран поиска игр будет реализован позже')),
    );
  }
}